# VLC Configuration and Usage

# Preventing multiple instances
Tools > Preferences > Interface > Instances > Allow only one instance

## Enqueue files
Optionally, tick Enqueue files when in one instance mode
